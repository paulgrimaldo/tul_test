# Tul Test

Import Postmand collection and Postman enviroment in order to test web services.

# How to Run
Create a database (MariaDB for example) with name *db_tultest* user: *root* and no password.

Package project with Maven

    mvn package

Run project with maven

	./mvnw spring-boot:run

